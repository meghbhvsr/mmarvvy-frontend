// We can change the name of this folder/file

const SecondPage = () => {
    return (
        <div>
            <h1>This is the main page that will show the search and the various vendor cards</h1>
        </div>
    );
}

export default SecondPage;